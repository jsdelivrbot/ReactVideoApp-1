# ReactVideoApp
ReactJS application that allows users to search for Youtube videos and view them.
Core react elements covered include: JSX, components, state.
Other elements covered: Youtube API, ES6, arrow functions, import/export statements 

App starts on index.js where the base component, App, is defined.
Index imports SearchBar, VideoDetail, VideoListItem and VideoList, and renders them within the App component.

